Paste this in the command prompt to activate the Virtual Environment. Swap 'Activate' with 'Deactivate' to, well, deactivate it.
env\Scripts\activate

Have to use .\env\Scripts\activate.ps1 with Visual Studio Code (OR REPLACE PS1 WITH BAT)

Page automatically updates with changes.

{% block head %} and etc. are a part of Jinja2.

To create the test.db file, go into the Env in Cmd, use Python shell, "import app from db" >> "db.create_all()"

Time in video: 43:03

You have to have the Procfile to have it run on heroku

When you're done with everything, "pip freeze > requirements"

Start the server by typing 'python app.py' inside of the env's cmd.
